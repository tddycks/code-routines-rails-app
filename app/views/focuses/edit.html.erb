Edit the Segment:

<%= render partial: '/workouts/errors', locals: { record: @focus } %>

<%= form_for ([@workout, @focus]) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br />
  <%= f.label :description %>
  <%= f.text_field :description %><br />
  <%= f.label "Number of Minutes" %>
  <%= f.number_field :duration %><br />
  <%= f.submit %>
<% end %>